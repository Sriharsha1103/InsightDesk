import { defineConfig } from 'vite';
// Uncomment the next line if you use React
// import react from '@vitejs/plugin-react';

export default defineConfig({
  // Uncomment the next line if you use React
  // plugins: [react()],
  server: {
    port: 3000, // Change as needed
  },
  // Add more configuration options as needed
});
